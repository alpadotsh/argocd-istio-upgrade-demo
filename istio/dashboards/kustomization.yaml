namespace: monitoring
configMapGenerator:
  - name: istio-grafana-dashboards
    options:
      labels:
        grafana_dashboard: "1"
    files:
      - istio-mesh-dashboard.json
      - istio-performance-dashboard.json
      - pilot-dashboard.json
  - name: istio-grafana-service-dashboard
    options:
      labels:
        grafana_dashboard: "1"
    files:
      - istio-service-dashboard.json
